<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.li.bbs.interaction.repository.InteractionRepository">

    <select id="existsByUserIdAndTargetTypeAndTargetIdAndActionType" resultType="boolean">
        SELECT COUNT(1) FROM interactions WHERE user_id = #{userId} AND target_type = #{targetType} AND target_id = #{targetId} AND action_type = #{actionType}
    </select>

    <insert id="insert" parameterType="com.li.bbs.interaction.domain.Interaction" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO interactions(user_id, target_type, target_id, action_type, created_at)
        VALUES(#{userId}, #{targetType}, #{targetId}, #{actionType}, NOW())
    </insert>

</mapper>

