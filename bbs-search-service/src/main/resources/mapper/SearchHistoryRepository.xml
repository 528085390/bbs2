<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.li.bbs.search.repository.SearchHistoryRepository">

    <insert id="insert" parameterType="com.li.bbs.search.domain.SearchHistory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO search_history(user_id, keyword, created_at)
        VALUES(#{userId}, #{keyword}, NOW())
    </insert>

    <select id="suggestByPrefix" resultType="string">
        SELECT keyword FROM search_history WHERE keyword LIKE CONCAT(#{prefix}, '%') GROUP BY keyword ORDER BY COUNT(*) DESC LIMIT 10
    </select>

</mapper>

