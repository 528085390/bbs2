<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.li.bbs.repository.RoleRepository">

    <resultMap id="RoleResultMap" type="com.li.bbs.domain.Role">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
    </resultMap>

    <select id="findByName" resultMap="RoleResultMap">
        SELECT id, name, description FROM roles WHERE name = #{name} LIMIT 1
    </select>

    <insert id="insert" parameterType="com.li.bbs.domain.Role" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roles(name, description) VALUES(#{name}, #{description})
    </insert>

</mapper>

